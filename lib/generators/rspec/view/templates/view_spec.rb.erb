require "rails_helper"

RSpec.describe Views::<%= class_name %>, type: :view do
  pending "renders without problem" do
    result = render_view(described_class.new)

    expect(result.css("h1").to_html).to eq "Hello World"
  end
end
